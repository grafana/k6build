name: CI
on:
  # Enable manually triggering this workflow via the API or web UI
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - v*
  pull_request:

jobs:
  checks:
    uses: grafana/k6-ci/.github/workflows/all.yml@a7138bd426a25e8472abf4e8b37f2ab9f09c866a # main
    with:
      skip-extension-testing: true
